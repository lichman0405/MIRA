# MIRA 环境变量配置示例
# 复制此文件为 .env 并根据需要修改

# 服务配置
APP_NAME=MIRA
DEBUG=false
HOST=0.0.0.0
PORT=8000

# 路径配置
DATA_DIR=./data
MODELS_CACHE_DIR=./models_cache
RESULTS_DIR=./results
STRUCTURES_DIR=./data/structures

# 模型配置
DEFAULT_DEVICE=cuda
DEFAULT_DTYPE=float32
# 启动时预加载的模型（逗号分隔）
# PRELOAD_MODELS=mace_prod_b3,orb_prod

# 任务配置
MAX_CONCURRENT_TASKS=4
TASK_TIMEOUT_SECONDS=86400

# 数据库配置（可选）
# DATABASE_URL=sqlite+aiosqlite:///./mira.db

# Redis 配置（可选，用于任务队列）
# REDIS_URL=redis://localhost:6379/0

# CORS 配置
# CORS_ORIGINS=["http://localhost:3000","http://localhost:8080"]
